<Page x:Class="GamothequeWPF.Views.StyleView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:GamothequeWPF.ViewModel"
      xmlns:local="clr-namespace:GamothequeWPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Background="White"
      Title="StyleView">
    <Page.DataContext>
        <vm:StyleViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150px"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Button Content="Ajouter" Command="{Binding AddNewType}"></Button>
            <Button Content="Enregistrer" Command="{Binding SaveType}"></Button>
            <Button Content="Annuler" Command="{Binding GetAllType}"></Button>
        </StackPanel>
        <Grid Grid.Column="2">
            <ItemsControl ItemsSource="{Binding AllTypes, Mode=TwoWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding Name}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</Page>
